import 'package:flutter/material.dart';

class MeetSponsors extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // Your FAQ screen UI here
    );
  }
}
