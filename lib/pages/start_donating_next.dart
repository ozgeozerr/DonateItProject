import 'package:flutter/material.dart';

class StartDonatingNext extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // Your FAQ screen UI here
    );
  }
}
